{
  "name": "dart-sector-analyzer-pages",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev public --compatibility-date=2025-12-01",
    "dev:node": "node scripts/dev-node-server.mjs",
    "deploy": "wrangler pages deploy public --project-name dart-sec-analyze",
    "check": "node scripts/check-pages-modules.mjs"
  },
  "dependencies": {
    "fflate": "^0.8.2"
  },
  "devDependencies": {
    "wrangler": "^4.50.0"
  }
}
